header
	p Booker
	p= current_user.present? ? "Sair" : "Acessar"


css:
	header{
		position:fixed;
		width: 100%;
		top: 0;
		left: 0;
		height: 40px;
		background: #333;
		padding: 0 2%;
		display: flex;
		flex-direction: row;
		justify-content: space-between;
	}
	header p{
		color: white;
		font-size: 1.3em;
		line-height: 40px;
	}